# replace with the public URL this cobalt instance will be reachable at
# (used for tunneling; wrong value breaks downloads)
API_URL=https://api.your-domain.example/

# host port to expose cobalt on
COBALT_PORT=9000
